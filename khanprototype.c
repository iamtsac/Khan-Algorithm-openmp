#include <stdio.h>


int main(){

    do{
        bool flag=false;
        int i=0;
        a=checkEdges();
        matrix[v][h]=0;
        tail[i]=a;

            for (int k=0; k<size; k++) {
                for (int l=0; l<size; l++) {
                    if (matrix[k][l]==1) {
                        flag=false;
                        printf("there are more edges");
                        break;
                    }
                }
            }
            i++
        }
    }while(!flag)
}

